version: 3
projects:
- name: k8s-platform
  dir: infra/terraform
  autoplan:
    when_modified: ["*.tf", "modules/**/*.tf"]
    enabled: true
  workflow: default

workflows:
  default:
    plan:
      steps:
      - init
      - plan
    apply:
      steps:
      - apply
